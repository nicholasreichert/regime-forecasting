# python
__pycache__/
*.pyc
.venv/
.ipynb_checkpoints/

# data
data/raw/*
data/processed/*
!data/raw/.gitkeep
!data/processed/.gitkeep

artifacts/results/*.csv
artifacts/results/latest_config.yaml
artifacts/regimes

# Study artifacts: keep the small summary tables that the paper is built from,
# drop the per-model prediction series (~14 MB) which are regenerable by
# `python -m src.experiments.run_study`.
artifacts/study/*/predictions/
artifacts/study/*/regimes/
artifacts/study/fixedK3_h5/

reports/figures/

.DS_Store
Thumbs.db
uv.lock
artifacts/study/multi_asset/predictions/

# LaTeX build intermediates (main.pdf is kept — it is the deliverable)
paper/*.aux
paper/*.log
paper/*.out
paper/*.bbl
paper/*.blg
paper/*.fls
paper/*.fdb_latexmk
paper/*.synctex.gz
